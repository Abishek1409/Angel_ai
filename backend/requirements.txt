django>=4.2,<5.0
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
python-dotenv>=1.0
gunicorn>=21.2
requests>=2.31.0
httpx==0.27.2

# Database
psycopg2-binary>=2.9
dj-database-url>=2.1

# AI and Vector DB - pinned versions for stability (avoid Rust/tokenizers build issues)
numpy==1.26.4
# Chroma 0.4.x uses the legacy PostHog telemetry client and can emit
# capture() signature errors even when telemetry is disabled.
chromadb==1.5.9

# Document processing
pymupdf==1.24.10
pytesseract>=0.3.13
pdf2image>=1.17.0

# Caching
redis==5.0.1
